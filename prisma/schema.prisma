generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Channel {
  id         Int    @id @default(autoincrement())
  channel_Id String
}
